import { Box, styled } from "@mui/material";

const DocumentWrapper = styled(Box)({
  minWidth: "325px",
});

export default function Document() {
  return <DocumentWrapper>Document</DocumentWrapper>;
}
