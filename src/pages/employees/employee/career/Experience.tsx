import { Box, styled } from "@mui/material";

const ExperienceWrapper = styled(Box)({
  minWidth: "325px",
});

export default function Experience() {
  return <ExperienceWrapper>Experience</ExperienceWrapper>;
}
