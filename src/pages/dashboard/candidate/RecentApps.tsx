import { Box, styled } from "@mui/material";

const RecentWrapper = styled(Box)({
  flex: 1,
  minHeight: "400px",
  padding: "10px",
});

export default function RecentApps() {
  return <RecentWrapper>RecentApps</RecentWrapper>;
}
