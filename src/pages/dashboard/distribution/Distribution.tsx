import { Box, styled } from "@mui/material";

const DistributionWrapper = styled(Box)({
  width: "100%",
  height: "100%",
});

export default function Distribution() {
  return <DistributionWrapper>Distribution</DistributionWrapper>;
}
