import { Box, styled } from "@mui/material";

const CandidateWrapper = styled(Box)({
  width: "100%",
  height: "100%",
});

export default function Candidate() {
  return <CandidateWrapper>Candidate</CandidateWrapper>;
}
