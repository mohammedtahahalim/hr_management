import { Box, styled } from "@mui/material";

const CollectionWrapper = styled(Box)({
  width: "100%",
  height: "100%",
});

export default function Collection() {
  return <CollectionWrapper>Collection</CollectionWrapper>;
}
