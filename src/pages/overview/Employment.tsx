import { Box, styled } from "@mui/material";

const EmployementWrapper = styled(Box)({
  minWidth: "350px",
  border: "1px solid white",
});

export default function Employment() {
  return <EmployementWrapper>Employment</EmployementWrapper>;
}
