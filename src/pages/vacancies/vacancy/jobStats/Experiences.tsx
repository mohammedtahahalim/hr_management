import { Box, styled } from "@mui/material";

const ExperiencesWrapper = styled(Box)({
  flex: 1,
  minWidth: "250px",
  border: "1px solid white",
});

export default function Experiences() {
  return <ExperiencesWrapper>Experiences</ExperiencesWrapper>;
}
